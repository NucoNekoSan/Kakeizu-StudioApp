import { Link } from "react-router-dom";
import { Notice } from "../../components/ui";
import { BACKUP_WARNING_DAYS } from "../backup/useBackup";
import { readStorageMode } from "../../storage/storageMode";

/**
 * アプリ内ヘルプ。
 *
 * 実装した安全機能（保存モード、全データ削除、暗号化バックアップ）は、
 * 使い方が伝わらなければ意味がないためここで説明する。
 * 警告日数は useBackup の定数を参照し、記述と実装がずれないようにする。
 */
export default function HelpPage() {
  const mode = readStorageMode();

  return (
    <div className="document-page">
      <article className="document">
        <header>
          <h1>使い方</h1>
        </header>

        <Notice tone="info">
          このアプリは、作成した相関図をこの端末のブラウザ内にのみ保存します。サーバーへ送信されることはありません。
        </Notice>

        <section>
          <h2>はじめかた</h2>
          <p>
            「新しい相関図」で相関図を作り、基準となる人物（本人）を追加します。以降は、選択中の人物を基準に「配偶者を追加」「子を追加」などから続柄を選ぶと、人物と関係線が自動で描かれます。
          </p>
          <p>
            続柄や性別の名称・線種・色は、設定画面から変更できます。使用頻度の低い続柄は既定で非表示になっているため、必要に応じて「選択肢に表示する」を有効にしてください。
          </p>
        </section>

        <section>
          <h2>保存方法の選び分け</h2>
          {mode && (
            <p className="document-revised">
              現在の設定:{" "}
              {mode === "persistent"
                ? "この端末に保存する"
                : "今回だけ使う（端末に残さない）"}
            </p>
          )}
          <ul>
            <li>
              <strong>この端末に保存する</strong> —
              自分専用の端末向けです。次に開いたときも続きから作業できます。
            </li>
            <li>
              <strong>今回だけ使う</strong> —
              共有の端末向けです。端末には一切保存されません。タブを閉じるか再読み込みすると作成した内容は消えるため、必要な内容はその都度書き出してください。
            </li>
          </ul>
          <p>設定画面の「バックアップ」タブからいつでも切り替えられます。</p>
        </section>

        <section>
          <h2>バックアップと復元</h2>
          <p>
            データはこの端末のブラウザ内にのみあります。端末の故障やブラウザのデータ削除で失われるため、定期的な書き出しをおすすめします。前回の書き出しから
            {BACKUP_WARNING_DAYS}日以上が経過すると、一覧画面でお知らせします。
          </p>
          <ul>
            <li>
              書き出しは設定画面の「バックアップ」タブから行います。JSONファイルとして保存されます。
            </li>
            <li>
              読み込みでは「追加する」と「置き換える」を選べます。置き換えると、今あるデータはすべて消えます。
            </li>
            <li>
              パスフレーズを設定すると、書き出したファイルを暗号化できます。パスフレーズを忘れると復元できません。
            </li>
            <li>
              別の端末で使う場合は、ファイルを移してから読み込んでください。自動的な同期は行いません。
            </li>
          </ul>
        </section>

        <section>
          <h2>共有の端末で使い終えたら</h2>
          <p>
            このアプリにはログアウトがありません。共有の端末では、次のいずれかを行ってください。
          </p>
          <ul>
            <li>最初に「今回だけ使う」を選んでおく（もっとも確実です）</li>
            <li>
              設定画面の「この端末のデータをすべて削除」を実行する（必要なら削除前に書き出せます）
            </li>
            <li>ブラウザのゲストモードやプライベートウィンドウで利用する</li>
          </ul>
          <p>
            書き出した画像やJSONファイルが端末に残っていないかも、あわせてご確認ください。
          </p>
        </section>

        <section>
          <h2>アプリとしてインストールする</h2>
          <p>
            EdgeやChromeのアドレスバーにあるインストールのアイコン、またはメニューの「アプリとしてインストール」を選ぶと、スタートメニューに登録され、独立したウィンドウで起動できます。インストール後はインターネットに接続していなくても利用できます。
          </p>
          <p>
            新しいバージョンを配信したときは画面上部にお知らせが出ます。編集中の内容が失われないよう、「更新する」を押したときにだけ切り替わります。
          </p>
        </section>

        <section>
          <h2>データが消える条件</h2>
          <ul>
            <li>ブラウザの「Cookieとサイトデータを削除」を実行したとき</li>
            <li>「今回だけ使う」でタブを閉じた、または再読み込みしたとき</li>
            <li>「この端末のデータをすべて削除」を実行したとき</li>
            <li>端末が故障した、またはブラウザを変更したとき</li>
          </ul>
          <p>いずれの場合も、書き出したJSONファイルがあれば復元できます。</p>
        </section>

        <section>
          <h2>第三者の情報を入力する場合</h2>
          <p>
            支援対象者やご依頼者など、本人以外の情報を入力する場合、その取得と利用が本人の同意、利用目的の範囲、所属機関の定めに照らして適法であることをご確認ください。書き出したファイルの保管方法も、所属機関の定めに従ってください。
          </p>
        </section>

        <section>
          <h2>外枠とPNGプレビュー</h2>
          <p>
            エディター上部の「外枠」から、画面の点線の表示とPNGの横幅・縦幅を設定できます。初期状態では点線は非表示ですが、人物の配置制限と保存PNGの点線は残ります。
          </p>
          <p>
            横幅・縦幅は図ごとに保存されます。外枠を小さくすると、はみ出す人物を内側へ移動するため、人物が重なる場合があります。
          </p>
          <p>
            「PNGプレビュー」で保存画像を確認できます。「このPNGを保存」は表示した画像をそのまま保存し、「編集に戻る」で調整を続けられます。
          </p>
        </section>
        <footer className="document-footer">
          <Link to="/terms">利用規約</Link>
          <Link to="/privacy">プライバシーポリシー</Link>
          <Link to="/charts">相関図に戻る</Link>
        </footer>
      </article>
    </div>
  );
}
